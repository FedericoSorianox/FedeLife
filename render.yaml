# ==================== CONFIGURACIÓN RENDER.COM ====================
# Archivo de configuración para despliegue automático en Render

services:
  # Servicio web principal
  - type: web
    name: fedelife-finanzas
    env: node
    runtime: node20
    plan: free
    buildCommand: |
      echo "🚀 Iniciando build personalizado..." &&
      npm cache clean --force &&
      npm ci --verbose --production=false &&
      echo "📋 Verificando dependencias críticas..." &&
      npm install critters autoprefixer postcss tailwindcss typescript --save-dev --force &&
      echo "🏗️ Construyendo aplicación Next.js..." &&
      npm run build &&
      echo "✅ Build completado exitosamente"
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: NODE_OPTIONS
        value: --max-old-space-size=512
      - key: MONGODB_URI
        value: mongodb+srv://fededanguard_db_user:CbBwcPzT5UatJlkT@cluster0.ew9wkss.mongodb.net/fede-life-finanzas?retryWrites=true&w=majority&appName=Cluster0
      - key: JWT_SECRET
        value: fede-life-super-secret-key-2024-production
      - key: JWT_EXPIRES_IN
        value: 365d
      - key: JWT_NO_EXPIRE
        value: false
      - key: RATE_LIMIT_WINDOW_MS
        value: 900000
      - key: RATE_LIMIT_MAX_REQUESTS
        value: 50
      - key: LOG_LEVEL
        value: error
      - key: FRONTEND_URL
        value: https://fedelife-finanzas.onrender.com
      - key: OPENAI_API_KEY
        fromSecret: openai_api_key

# Nota: Usando MongoDB Atlas en lugar de base de datos de Render
# databases:
#   - name: fede-life-mongodb
#     databaseName: fedelife_finanzas
#     plan: free
